#include <stdio.h>
#include <stdlib.h>
#include <string.h>
char* copyString(char s[])
{
	char* s2;
	s2 = (char*)malloc(20);

	strcpy(s2, s);
	return (char*)s2;
}
int main()
{
    printf("radhika-192210126");
	char s1[20] = "SIMATS";
	char* s2;
	s2 = copyString(s1);
	printf("%s", s2);
	return 0;
}
